# Analytics endpoint — inactive draft

`analytics.js.txt` preserves the supplied snippet. It is not imported or mounted.

All counts, funnel values, and timeline entries are simulated examples, not measured restaurant activity. They must not be presented as live analytics.

Analytics does not technically depend on Stripe. Before activation, implement authorized per-restaurant access, real event collection and aggregation, an agreed response contract, and frontend error handling. Keep any demonstration data explicitly labelled and separate from production metrics.